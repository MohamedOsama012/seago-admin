import 'package:flutter/material.dart';

abstract class WegoColors {
  static const mainColor = Color(0xff0AA6A5);
  static const cardColor = Color.fromRGBO(242, 250, 250, 1);
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;
}
